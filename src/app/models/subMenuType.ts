export const DrawTypes = {
  brush: 'brush',
  shape: 'shape'
} as const;

export enum SubMenuType {
  drawBrush='drawBrush',
  drawShape='drawShape',
  text = 'text',
  filter = 'filter',
  crop='crop',
  none = 'none'
}
